"use client";

import React, { useEffect, useRef, useState } from "react"


export default function ChatbotPage() {
    // const session = useSession();
    return (
        <div className="relative h-full p-4 lg:pl-4 md:pl-0 pl-4 bg-white opacity-0 animate-fadeIn">
            <h1 className="text-2xl font-bold mb-4">Chatbot Page welcome</h1>
        </div>
    );
}
